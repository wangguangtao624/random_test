# CIS芯片TRNG随机性测试套件 - 完成说明

## 项目概述

本工具已成功创建，集成了 **NIST SP 800-22** 和 **GB/T 32915-2016** 两个标准的全部测试项，用于验证CIS芯片真随机数生成器(TRNG)的输出质量。

## 测试覆盖情况

### NIST SP 800-22 (15项)
✓ 全部15项测试已实现

### GB/T 32915-2016 补充测试 (3项)
✓ 全部3项测试已实现

**总计：18项测试，100%覆盖两个标准**

## 项目结构

```
random_test_suite/
├── README.md                    # 项目说明
├── 快速开始.md                  # 快速入门指南
├── 使用指南.md                  # 详细使用指南
├── 项目结构说明.md              # 项目结构说明
├── 完成说明.md                  # 本文件
├── requirements.txt             # Python依赖列表
├── install.bat                  # Windows安装脚本
├── main.py                      # 主程序入口
├── test_tool.py                 # 工具验证脚本
├── quick_test.py                # 快速测试脚本
├── generate_sample_data.py      # 示例数据生成脚本
├── config.py                    # 配置文件
├── core/                        # 核心测试模块
│   ├── __init__.py
│   ├── nist_tests.py            # NIST测试实现
│   ├── gbt_tests.py             # GB/T测试实现
│   └── statistics.py            # 统计分析函数
├── utils/                       # 工具模块
│   ├── __init__.py
│   ├── data_loader.py           # 数据加载器
│   └── report_generator.py      # 报告生成器
├── data/                        # 测试数据目录
└── reports/                     # 测试报告目录
```

## 快速开始

### 1. 安装依赖
```bash
pip install numpy scipy matplotlib
```

### 2. 准备测试数据
将CIS芯片的TRNG输出数据放入 `data/` 目录。

### 3. 运行测试
```bash
python main.py --data-dir ./data
```

### 4. 查看报告
测试完成后，在 `reports/` 目录下查看测试报告。

## 功能特性

### 数据加载
- 支持多种格式：.bin, .txt, .dat, .raw
- 支持批量加载目录中的文件
- 自动验证数据格式和长度
- 支持序列分割和信息提取

### 测试执行
- NIST SP 800-22 全部15项测试
- GB/T 32915-2016 补充3项测试
- 支持自定义测试参数
- 自动计算通过率

### 报告生成
- 文本报告（.txt）
- HTML报告（.html，含图表）
- JSON结果（.json）
- 自动生成可视化图表

## 测试项详解

### NIST SP 800-22 测试（15项）

| 序号 | 测试名称 | 检测目的 |
|------|----------|----------|
| 1 | frequency_monobit | 0和1的比例是否接近50:50 |
| 2 | frequency_within_block | 块内0/1比例是否均匀 |
| 3 | runs | 游程数量是否符合随机预期 |
| 4 | longest_run_of_ones | 最长连续1的长度分布 |
| 5 | binary_matrix_rank | 矩阵秩分布，检测线性依赖 |
| 6 | dft_spectral | 检测周期性特征 |
| 7 | non_overlapping_template | 特定模式出现频率 |
| 8 | overlapping_template | 重叠模式出现频率 |
| 9 | maurers_universal | 检测序列是否可压缩 |
| 10 | linear_complexity | LFSR长度检测 |
| 11 | serial | 所有m位模式频率 |
| 12 | approximate_entropy | 序列熵值检测 |
| 13 | cumulative_sums | 偏移的最大累积和 |
| 14 | random_excursions | 循环游程中各状态访问次数 |
| 15 | random_excursions_variant | 特定状态访问频率 |

### GB/T 32915-2016 补充测试（3项）

| 序号 | 测试名称 | 检测目的 |
|------|----------|----------|
| 16 | poker_test_m4 | 4位子序列的出现频率 |
| 17 | poker_test_m8 | 8位子序列的出现频率 |
| 18 | autocorrelation_test | 序列与其移位版本的相关性 |
| 19 | binary_derivative_test | 逐级差分后0/1比例 |

## 通过标准

| 指标 | 要求 | 说明 |
|------|------|------|
| 单项测试P-value | ≥ 0.01 | 每项测试的最低要求 |
| 总体通过率 | ≥ 98% | 所有测试的平均通过率 |
| 测试序列长度 | ≥ 1Mbit/组 | 每组数据的最小长度 |
| 测试序列组数 | ≥ 1000组 | 统计意义的最小样本量 |

## 使用示例

### 测试单个文件
```bash
python main.py --file data/sample.bin
```

### 测试整个目录
```bash
python main.py --data-dir ./data
```

### 指定输出目录和格式
```bash
python main.py --data-dir ./data --output ./reports --format html
```

### 生成示例数据
```bash
python generate_sample_data.py --output ./data --count 10 --length 1000000
```

### 快速验证工具
```bash
python quick_test.py
```

## 注意事项

1. **数据质量**：确保测试数据是从CIS芯片正确采集的TRNG输出
2. **数据长度**：每个文件至少包含1,000,000比特（1Mbit）
3. **样本数量**：建议至少1000个文件用于统计分析
4. **采集条件**：多批次、不同时段采集，覆盖温度/电压变化

## 后续步骤

1. 将CIS芯片的TRNG输出数据采集到文件
2. 将数据文件放入 `data/` 目录
3. 运行测试：`python main.py --data-dir ./data`
4. 查看 `reports/` 目录下的测试报告
5. 根据测试结果评估TRNG质量

## 技术支持

如有问题，请参考：
- `README.md` - 项目说明
- `快速开始.md` - 快速入门指南
- `使用指南.md` - 详细使用指南
- `项目结构说明.md` - 项目结构说明

## 版本信息

- 版本：1.0.0
- 日期：2026-05-25
- 功能：完整实现NIST SP 800-22和GB/T 32915-2016全部测试项
